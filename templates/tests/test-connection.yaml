apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onm-service.fullname" . }}-test-connection"
  labels:
    {{- include "onm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
